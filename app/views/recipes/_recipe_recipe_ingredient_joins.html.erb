<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Recipe Ingredient Joins

    <a data-toggle="collapse" href="#recipe_recipe_ingredient_joins_new_form">
      Add recipe ingredient join
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @recipe.recipe_ingredient_joins.each do |recipe_ingredient_join| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to recipe_ingredient_join, recipe_ingredient_join %>

        <%= link_to recipe_ingredient_join, method: :delete, data: { confirm: "Do you really want to delete this recipe_ingredient_join" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="recipe_recipe_ingredient_joins_new_form">
        <%= render partial: "recipes/recipe_recipe_ingredient_joins_new_form" %>
      </div>
    </div>

  </ul>
</div>
